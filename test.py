import numpy as np

#w = input("w = ")

#w = list(map(float,w.split(",")))
# map use to convert list element type

n = 4
prior = np.zeros((n,1))
print(prior)
cov = np.identity(n)
print(cov)

x = 0.5



